<?php 

/**
 * Implements hook_theme().
 */
function travel_theme($existing, $type, $theme, $path) {
    
    return array(
        'privacy_policy' => array(
            'variables' => array('title' => '', 'content' => '')
        )
    );
}